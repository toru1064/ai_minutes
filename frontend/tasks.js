import {getCurrentUser,handleSigninCallback,login,logout} from "./auth.js";
const api="https://ba2lg9ckm9.execute-api.ap-northeast-1.amazonaws.com/tasks";
const labels={not_started:"未着手",in_progress:"対応中",review_pending:"確認待ち",completed:"完了",rejected:"差し戻し",low:"低",normal:"通常",high:"高",urgent:"緊急"};
const status=document.getElementById("user-status"), loginButton=document.getElementById("login-button"), logoutButton=document.getElementById("logout-button"), message=document.getElementById("list-message"), body=document.getElementById("tasks-table-body");
async function init(){await handleSigninCallback();const user=await getCurrentUser();if(!user||user.expired){status.textContent="ログインしていません";loginButton.hidden=false;return;}status.textContent=`ログイン中：${user.profile.email}`;logoutButton.hidden=false;document.getElementById("tasks-section").hidden=false;try{const response=await fetch(api,{headers:{Authorization:`Bearer ${user.access_token}`}}),data=await response.json();if(!response.ok)throw new Error(data.message);const tasks=data.tasks||[];message.textContent=tasks.length?`${tasks.length}件を表示`:"登録されたチケットはありません";for(const task of tasks){const row=document.createElement("tr");for(const value of [`#${task.task_number}`,task.project_name,task.title,task.assignee,task.due_date,labels[task.status],labels[task.priority]]){const cell=document.createElement("td");cell.textContent=value||"-";row.appendChild(cell);}const cell=document.createElement("td"),link=document.createElement("a");link.href=`task_detail.html?id=${encodeURIComponent(task.task_id)}`;link.textContent="表示";link.className="table-action link-button";cell.appendChild(link);row.appendChild(cell);body.appendChild(row);}}catch(error){message.textContent=error.message;}}
loginButton.addEventListener("click",login);logoutButton.addEventListener("click",logout);init();
